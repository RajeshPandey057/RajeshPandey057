#!/bin/bash
a=( 20 10 65 43 23 )
echo ${a[@]}
i=0
while [ $i -lt 5 ]
do 
	k=$i
	while [ $k -lt 5 ]
	do
		if [ ${a[$i]} -gt ${a[$k]} ]
		then
			temp=${a[$i]}
			a[$i]=${a[$k]}
			a[$k]=$temp
		fi
		k=`expr $k + 1`
	done
	i=` expr $i + 1 `
done
echo ${a[@]}
